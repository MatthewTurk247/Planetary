//
//  TranslateString.swift
//  Planetary
//
//  Created by Matthew Turk on 8/22/17.
//  Copyright © 2017 MonitorMOJO. All rights reserved.
//

/*import Foundation
import UIKit
import ROGoogleTranslate

private let PSApiKey = "AIzaSyDJLXP9zaEi66E5ChKUN2XUfVwlnfL4ldE"
var params = ROGoogleTranslateParams()
let translator = ROGoogleTranslate()

extension UILabel {
    
    func loadUsingTranslation(_ text: String, ln: String, fallback: String) {
        self.text = fallback
        params.source = "en"
        params.target = ln
        translator.apiKey = PSApiKey
        params.text = text
        DispatchQueue.main.async(execute: {
            translator.translate(params: params) { (result) in
                print("Translation: \(result)")
                self.text = result
            }
        })
    }
    
}*/
